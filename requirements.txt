fastapi
uvicorn[standard]
python-multipart
pydantic
pydantic-settings
httpx

# OCR (V1: local, self-contained — no cloud credentials required)
pytesseract
Pillow
PyMuPDF

# Persistence (optional at runtime via DATABASE_URL; Railway Postgres compatible)
SQLAlchemy>=2.0
psycopg2-binary
